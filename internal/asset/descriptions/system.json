{
  "clipboard_write": {
    "desc": "Write text to the system clipboard (macOS/Linux/Windows).",
    "params": {
      "text": "The text to copy to the clipboard."
    }
  },
  "clipboard_read": {
    "desc": "Read text from the system clipboard (macOS/Linux/Windows). Returns the current clipboard contents.",
    "params": {}
  },
  "http_request": {
    "desc": "Make an HTTP request. Returns status code, response body (JSON auto-pretty-printed), and timing. Response body is capped at 1 MB. Timeout max: 300 s. Cross-platform — prefer this over running curl/wget via run_command.",
    "params": {
      "url": "The full URL to request.",
      "method": "HTTP method: GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS. Default: GET.",
      "headers": "Request headers as a JSON object, e.g. {\"Authorization\": \"Bearer token\"}.",
      "body": "Request body. For JSON APIs, pass a JSON string.",
      "basic_auth": "Basic auth in \"username:password\" format.",
      "timeout_seconds": "Request timeout in seconds. Default: 30.",
      "follow_redirects": "Follow HTTP redirects. Default: true.",
      "include_response_headers": "Include response headers in output. Default: false."
    }
  },
  "list_processes": {
    "desc": "List running processes with PID, name, CPU/memory usage, and full command. Cross-platform — prefer this over running ps/tasklist via run_command. On POSIX (macOS/Linux) CPU and MEM are percentages from ps; on Windows CPU is total CPU time in seconds and MEM is working-set in MB — column headers reflect this automatically. Optionally filter by name.",
    "params": {
      "filter": "Case-insensitive substring to filter by name or command. Omit to list all.",
      "sort_by": "Sort by \"pid\" (default), \"cpu\", or \"mem\".",
      "limit": "Maximum processes to return. Default: 50."
    }
  },
  "get_system_info": {
    "desc": "Return system resource snapshot: CPU model, core count, memory usage, and disk usage.",
    "params": {}
  },
  "download_file": {
    "desc": "Download a file from a URL and save it to a local path. Supports large files with streaming (no memory buffering). Creates parent directories automatically. Returns file size and elapsed time. Prefer this over http_request + write_file for binary or large downloads.",
    "params": {
      "url": "The URL to download from",
      "path": "Local file path to save to. Creates parent directories automatically.",
      "headers": "Optional request headers as a JSON object, e.g. {\"Authorization\": \"Bearer token\"}.",
      "timeout_seconds": "Request timeout in seconds. Default: 300, max: 600.",
      "overwrite": "Overwrite if file already exists. Default: false."
    }
  }
}
