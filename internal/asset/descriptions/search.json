{
  "search_files": {
    "desc": "Find files and directories whose names match a glob pattern. Supports *, **, ?, and {a,b} alternation. Patterns without / match filename only; patterns with / or ** match full relative path.",
    "params": {
      "path": "Root directory to search from. Defaults to the server's current working directory.",
      "pattern": "Glob pattern (e.g. *.go, **/*.json, {src,lib}/**/*.ts)",
      "recursive": "Search subdirectories recursively. Default: true",
      "max_results": "Maximum results to return. Default: 100",
      "show_hidden": "Include hidden files/directories. Default: false"
    }
  },
  "grep_files": {
    "desc": "Search file contents for a pattern. Returns matching lines with file path and line number. Supports literal strings and Go regex, context lines, pagination, glob filtering, multiline patterns, and three output modes (content / files_with_matches / count).",
    "params": {
      "path": "File or directory to search. Defaults to the server's current working directory.",
    "pattern": "Literal text or regular expression. For regex syntax (|, ., *, +, ^, $, [], \\w, \\d, etc.) set use_regex=true; otherwise the pattern is matched as a literal string.",
    "use_regex": "Treat pattern as a Go regular expression. Default: false. Required for alternation (|), wildcards (., +, *), anchors (^, $), and character classes.",
      "case_insensitive": "Case-insensitive matching. Default: false",
      "recursive": "Search subdirectories recursively. Default: true",
      "context_lines": "Context lines before and after each match. Default: 0. Max: 50.",
      "max_results": "Max matches per page (content mode). Default: 50.",
      "offset": "Skip the first N matches (for pagination). Default: 0",
      "glob": "Only search files matching this glob pattern (e.g. *.go, *.{ts,tsx}). Default: all files",
      "output_mode": "Output format. Default: \"content\". Options: \"content\" (matching lines with line numbers), \"files_with_matches\" (only file paths), \"count\" (match counts per file).",
      "show_hidden": "Include hidden files. Default: false",
      "multiline": "Enable multiline patterns that span lines. Default: false",
      "max_file_size": "Skip files larger than this many bytes. Default: 0 (no limit)."
    }
  }
}
