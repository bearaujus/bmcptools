{
  "list_directory": {
    "desc": "List directory contents with sizes and modification times. Supports recursion and glob filtering.",
    "params": {
      "path": "Directory to list. Defaults to the server's current working directory.",
      "show_hidden": "Include hidden entries (names starting with '.'). Default: false",
      "recursive": "List subdirectories recursively. Default: false",
      "max_depth": "Max recursion depth when recursive=true. Default: 3",
      "sort_by": "Sort by \"name\" (default) or \"size\" (largest first).",
      "glob": "Only show files matching this glob pattern (e.g. *.go, *.{ts,tsx}). Default: all files"
    }
  },
  "create_directory": {
    "desc": "Create a directory and all missing parents (mkdir -p). No-op if already exists.",
    "params": {
      "path": "Directory path to create"
    }
  },
  "delete_directory": {
    "desc": "Delete a directory. Must be empty unless force=true.",
    "params": {
      "path": "Directory path to delete",
      "force": "Delete non-empty directories recursively. Default: false"
    }
  },
  "directory_tree": {
    "desc": "Show a recursive visual tree of files and directories with sizes.",
    "params": {
      "path": "Root directory to display",
      "max_depth": "Maximum recursion depth. Default: 3",
      "show_hidden": "Include hidden files/directories. Default: false",
      "exclude_patterns": "Glob patterns to exclude (e.g. [\"*.log\", \"node_modules\"])",
      "glob": "Only show files matching this glob pattern (e.g. *.go). Default: all files"
    }
  }
}
